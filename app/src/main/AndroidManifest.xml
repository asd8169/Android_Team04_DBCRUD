<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidlec.databasequiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DatabaseQuiz">
        <activity android:name=".MainActivity" android:label="학생 정보 관리">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    <activity android:name=".splash"/>
        <activity android:name=".MemberDelect" android:label="학생 정보 삭제"></activity>
        <activity android:name=".MemberSelect" android:label="학생 정보 목록"/>
        <activity android:name=".MemberUpdate" android:label="학생 정보 수정"/>
        <activity android:name=".MemberInsert" android:label="학생 정보 입력"/>


    </application>

</manifest>